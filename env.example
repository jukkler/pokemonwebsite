# ================================
# Pokemon Website - Docker Environment
# ================================
# Kopiere diese Datei zu .env und passe die Werte an:
# cp env.example .env

# ================================
# PostgreSQL Database
# ================================
POSTGRES_USER=pokemon
POSTGRES_PASSWORD=pokemon
POSTGRES_DB=pokemon

# ================================
# Application
# ================================
# Database URL (muss mit den PostgreSQL-Werten übereinstimmen)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://pokemon:pokemon@db:5432/pokemon

# Session Secret (mindestens 32 Zeichen, zufällig generieren!)
# Generieren mit: openssl rand -base64 32
SESSION_SECRET=CHANGE-ME-minimum-32-characters-long-secret-key

# Admin Zugangsdaten
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE-ME-secure-password

# ================================
# Optional: Externe Datenbank
# ================================
# Wenn du eine externe PostgreSQL-Datenbank nutzen möchtest,
# kommentiere die POSTGRES_* Variablen oben aus und setze
# DATABASE_URL auf deine externe Datenbank-URL.
# Entferne auch den db-Service aus docker-compose.yml
