# ================================
# Pokemon Website - Docker Environment
# ================================
# Kopiere diese Datei zu .env und passe die Werte an:
# cp env.example .env
#
# ⚠️  SICHERHEITSHINWEIS: Ändere ALLE Standardwerte vor dem Deployment!

# ================================
# PostgreSQL Database
# ================================
# Generiere sichere Passwörter mit: openssl rand -base64 24
POSTGRES_USER=pokemon
POSTGRES_PASSWORD=CHANGE-ME-use-openssl-rand-base64-24
POSTGRES_DB=pokemon

# ================================
# Application
# ================================
# Database URL (muss mit den PostgreSQL-Werten übereinstimmen)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://pokemon:CHANGE-ME-use-openssl-rand-base64-24@db:5432/pokemon

# Session Secret (mindestens 32 Zeichen, zufällig generieren!)
# Generieren mit: openssl rand -base64 32
SESSION_SECRET=CHANGE-ME-minimum-32-characters-long-secret-key

# Admin Zugangsdaten
# ⚠️  Verwende ein starkes Passwort! Niemals "admin" in Production!
# Generiere mit: openssl rand -base64 16
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE-ME-secure-password

# ================================
# Checkliste vor Deployment
# ================================
# [ ] POSTGRES_PASSWORD geändert
# [ ] DATABASE_URL mit neuem Passwort aktualisiert
# [ ] SESSION_SECRET generiert (openssl rand -base64 32)
# [ ] ADMIN_PASSWORD geändert (nicht "admin"!)
# [ ] .env Datei NIEMALS in Git committen!

# ================================
# Optional: Externe Datenbank
# ================================
# Wenn du eine externe PostgreSQL-Datenbank nutzen möchtest,
# kommentiere die POSTGRES_* Variablen oben aus und setze
# DATABASE_URL auf deine externe Datenbank-URL.
# Entferne auch den db-Service aus docker-compose.yml
